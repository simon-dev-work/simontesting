import NotFound from './components/NotFound';

export default function NotFoundPage() {
  return <NotFound />;
}

export const metadata = {
  title: 'Page Not Found | Lumina Blue',
  description: 'The page you are looking for does not exist.',
};
